info:
  name: Nicht-vorhandene Property
  type: http
  seq: 4

http:
  method: GET
  url: "{{restUrl}}?strasse=10"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: strasse
      value: "10"
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
